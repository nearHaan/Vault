import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.height;
  static double screenWidth = Get.width;

  static double bigThinfontSize = screenHeight*0.035;
  static double bigBoldfontSize = screenHeight*0.03;
  static double normalBoldfontSize = screenHeight*0.02;
  static double smallfontSize = screenHeight*0.015;
  static double bigPasswordfontSize = screenHeight*0.08;

  static double radius10 = screenWidth*0.03;
  static double radius20 = screenWidth*0.06;
  static double passWinPad = screenWidth*0.15;
  static double xPad = screenWidth*0.07;
  static double xxPad = screenWidth*0.05;
}